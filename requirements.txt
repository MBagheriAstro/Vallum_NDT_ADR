# Adafruit Motor HAT (PCA9685) on Jetson via I2C.
# Requires I2C enabled and HAT on the 40-pin header.
# Jetson.GPIO is required by Blinka for board detection on Jetson.
Jetson.GPIO
adafruit-blinka
Adafruit-PlatformDetect
adafruit-circuitpython-motorkit
adafruit-circuitpython-pca9685
adafruit-circuitpython-motor

# Web application stack for Jetson dashboard
fastapi
uvicorn[standard]
jinja2
pydantic

# Inspection inference (YOLO + ball extraction).
# Version constraints match VALLUM-NDT-ADR-BACKEND (setup_jetson.sh, src/training/yolo/requirements.txt).
# On Jetson: run scripts/setup_jetson.sh once to install PyTorch/torchvision from NVIDIA wheels (GPU).
numpy>=1.21.0
opencv-python>=4.5.0
Pillow>=9.0.0
ultralytics>=8.0.0
